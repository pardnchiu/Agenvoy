{
  "name": "agenvoy-site",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "sync-tags": "node sync-tags.js",
    "dev": "node build.js && wrangler dev",
    "deploy": "node build.js && wrangler deploy"
  },
  "dependencies": {
    "marked": "^12.0.2"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
