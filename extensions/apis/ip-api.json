{
  "name": "ip_location",
  "description": "[system-default] Get geolocation details for an IP address, or the local IP when omitted.",
  "always_allow": true,
  "endpoint": {
    "url": "http://ip-api.com/json/{ip}",
    "method": "GET",
    "content_type": "json"
  },
  "parameters": {
    "ip": {
      "type": "string",
      "description": "IPv4 or IPv6 address to look up. Leave empty to use the local IP.",
      "required": false,
      "default": ""
    }
  },
  "response": {
    "format": "json"
  }
}
