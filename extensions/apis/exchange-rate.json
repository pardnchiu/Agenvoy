{
  "name": "exchange_rate",
  "description": "Get real-time exchange rates for the specified base currency.",
  "endpoint": {
    "url": "https://open.er-api.com/v6/latest/{currency}",
    "method": "GET",
    "content_type": "json"
  },
  "parameters": {
    "currency": {
      "type": "string",
      "description": "Base currency code in ISO 4217 format, for example USD, TWD, JPY, or EUR.",
      "required": true,
      "enum": ["USD", "TWD", "JPY", "EUR", "GBP", "CNY", "KRW", "HKD", "SGD", "AUD"]
    }
  },
  "response": {
    "format": "json"
  }
}
