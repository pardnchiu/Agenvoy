{
  "name": "threads_publish_image",
  "description": "Publish a Threads post with a single image.",
  "parameters": {
    "type": "object",
    "properties": {
      "image_url": {
        "type": "string",
        "description": "Public URL of the image (JPEG or PNG, up to 8MB)."
      },
      "text": {
        "type": "string",
        "description": "Caption text, up to 500 characters.",
        "default": ""
      }
    },
    "required": ["image_url"]
  }
}
