{
  "name": "threads-refresh-token",
  "description": "Refresh the Threads long-lived access token and update the local keychain entry.",
  "parameters": {
    "type": "object",
    "properties": {}
  }
}
