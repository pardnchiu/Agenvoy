{
  "name": "threads_publish_text",
  "description": "Publish a text-only post to Threads.",
  "parameters": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "Post content, up to 500 characters."
      }
    },
    "required": ["text"]
  }
}
