{
  "name": "example_greet_js",
  "description": "向指定名字的人打招呼，回傳問候訊息（Javascript 版）",
  "parameters": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "要打招呼的名字"
      }
    },
    "required": ["name"]
  }
}
