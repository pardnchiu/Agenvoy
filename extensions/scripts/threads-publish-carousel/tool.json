{
  "name": "threads_publish_carousel",
  "description": "Publish a carousel post to Threads with 2 to 20 images or videos.",
  "parameters": {
    "type": "object",
    "properties": {
      "items": {
        "type": "array",
        "description": "Carousel items, 2 to 20 entries. Each entry must include media_type and the matching url.",
        "items": {
          "type": "object",
          "properties": {
            "media_type": {
              "type": "string",
              "enum": ["IMAGE", "VIDEO"],
              "description": "Media type for this carousel item."
            },
            "url": {
              "type": "string",
              "description": "Public URL of the image or video."
            }
          },
          "required": ["media_type", "url"]
        },
        "minItems": 2,
        "maxItems": 20
      },
      "text": {
        "type": "string",
        "description": "Caption text, up to 500 characters.",
        "default": ""
      }
    },
    "required": ["items"]
  }
}
