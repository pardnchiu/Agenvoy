{
  "name": "yt-dlp-info",
  "description": "Get video metadata and available download formats with yt-dlp without downloading the media.",
  "parameters": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "Video URL to inspect."
      },
      "list_formats": {
        "type": "boolean",
        "description": "List all available formats and recommended format strings when true.",
        "default": false
      }
    },
    "required": ["url"]
  }
}
