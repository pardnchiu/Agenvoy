{
  "name": "example-yt-dlp-info",
  "description": "查詢影片 metadata（標題、頻道、時長、縮圖、上傳日期）及可用下載格式，不實際下載。在呼叫 yt-dlp-downloader 前先用此工具確認格式選項。",
  "parameters": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "要查詢的影片 URL"
      },
      "list_formats": {
        "type": "boolean",
        "description": "是否列出所有可用格式及推薦的 format 字串，預設 false",
        "default": false
      }
    },
    "required": ["url"]
  }
}
